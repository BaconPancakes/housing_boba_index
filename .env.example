# Google Places API Key — https://console.cloud.google.com/apis/credentials
GOOGLE_PLACES_API_KEY=

# Scrape Google Maps HTML instead of calling the paid API.
# Auto-enabled when no API key is set.  Set to 0 to force demo mode.
# USE_SCRAPER=1
